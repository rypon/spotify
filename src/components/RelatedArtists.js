import React from "react";

function RelatedArtists({ related }) {
  // const allRelated = related.map((artist, i) => {

  // })
  // let relatedArtists = related?.artists;
  // console.log(relatedArtists);
  return <div>RelatedArtists</div>;
}

export default RelatedArtists;
